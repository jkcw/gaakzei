$(document).ready(function() {
    win_resize();
});

function win_resize() {
    var h = $(window).height();
    var w = $(window).width();
    $(".latest-products-title").html(w);
    return w;
}

function show_suitable_ads() {
    var w = win_resize();
    var op = '';

    if (w > ) {

    }
}